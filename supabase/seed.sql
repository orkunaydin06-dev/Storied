-- Local dev seed data
-- Run after migrations to get a testable state

-- Insert a test user (requires auth.users entry first — created via Supabase auth in dev)
-- This seed is intentionally minimal; use the app UI to create real data

-- Uncomment and fill in your test user UUID after creating via Supabase dashboard:
-- insert into public.users (id, email, first_name, current_day, streak_days, paid_at, amount_paid_cents, pricing_tier)
-- values (
--   'your-test-user-uuid',
--   'test@example.com',
--   'Test',
--   0,
--   0,
--   now(),
--   2900,
--   'founding'
-- );
